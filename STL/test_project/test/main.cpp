#include<iostream>
#include<queue>
#include<map>
#include<vector>
#include<string>

using namespace std;

int main()
{
    int length = 1;
    string s;
    cin>>s;
    int x=0;
    map<char,char> mymap;
    int j=1;
    mymap.insert(s[0],s[0]);
    mymap.insert(s[1],s[1])
    while(j!=s.length())
    {
        if()
    }


/*
    string s;
    cin>>s;
    char c;
    map<char,char> table;


    for(int i=0;i<s.length();++i)
    {
        table.insert(make_pair(s[i],s[i]));


    }
    map<char,char>::iterator it=table.begin();

    it=table.find('a');
    int x = (*it).second;
    cout<<x;

*/
    return 0;
}
